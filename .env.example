# D-ID API Keys (comma-separated for round-robin rotation)
# Get your free API key at: https://studio.d-id.com/ (Settings → API Keys)
DID_API_KEYS=your_did_api_key_here

# You can add multiple keys for round-robin:
# DID_API_KEYS=key1,key2,key3
